/** Grid tile — 4:3 cards, max ~half column on sm+ */
export const tourTileSizes =
	'(max-width: 639px) calc(50vw - 1.5rem), (max-width: 1023px) calc((min(100vw, 1240px) - 2rem) / 4), calc((min(100vw, 1240px) - 2rem) / 4)';

export const tourTileQuality = 68;
export const tourTileWidth = 360;
export const tourTileWidth2x = 480;
